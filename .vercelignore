# Files that should never be uploaded to Vercel.
# (dist/ is intentionally NOT ignored: it's produced by the build command
# and the api/server.ts function depends on dist/server/** at runtime.)

node_modules
.git
.wrangler
.dev.vars
.tanstack
.stitch
*.log
.DS_Store
